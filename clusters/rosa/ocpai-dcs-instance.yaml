apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ocpai-default-dsc
  namespace: openshift-gitops
  labels:
    app.kubernetes.io/part-of: platform
  annotations:
    # Ensure this deploys after any future "cluster-basics" bundles if you add them.
    argocd.argoproj.io/sync-wave: "20"
spec:
  project: finsight-project
  source:
    repoURL: 'https://github.com/finsignt-demojam/mono-repo'
    targetRevision: main
    path: components/platform/ocpai/base
  destination:
    server: https://kubernetes.default.svc
    # namespace: redhat-ods-operator   # This is cluster scoped
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      # Let Argo create the target namespace for Node Feature Discovery Instance.
      - CreateNamespace=false
      # Optional but useful in day-2:
      - ApplyOutOfSyncOnly=true
  revisionHistoryLimit: 5
